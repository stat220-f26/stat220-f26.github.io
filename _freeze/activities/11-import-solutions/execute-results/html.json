{
  "hash": "90494b0860f606ee9ae62af51ad39581",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Data import and dates/times -- Solutions\"\n---\n\n\n\nClick the \"code\" button above to copy and paste the source code\n\n# Warm Up\n\nUse `read_csv()` to import the `desserts` data set from\n\n- `https://stat220-s25.github.io/data/desserts.csv`\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndesserts <- read_csv(\"https://stat220-s25.github.io/data/desserts.csv\")\n```\n:::\n\n\n# Your turn\n\nUse the appropriate `read_<type>()` function to import the following data sets:\n\n-   `https://stat220-s25.github.io/data/data-4.csv`\n\n-   `https:/stat220-s25.github.io/data/tricky-1.csv`\n\n\n\nIf you hit any errors/problems, be sure to explore them and identify the issue, even if you can't \"fix\" it.\n\n### data-4.csv\n\nOpening the URL for `data-4.csv` you see that the delimiter is `|`, so you should use `read_delim()`:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndata4 <- read_delim(\"https://stat220-s25.github.io/data/data-4.csv\", delim = \"|\")\n```\n:::\n\n\n### tricky-1.csv\n\nAt first glance it isn't obvious what is tricky about `tricky-1.csv`; however, if you try to use `read_csv()` you'll hit the following problem:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntricky1 <- read_csv(\"https://stat220-s25.github.io/data/tricky-1.csv\")\nproblems(tricky1)\n```\n:::\n\n\nThe output of `problems()` here is that in rows 4 and 7 `read_csv` only finds 4 columns, but 5 are expected. Looking at the imported data set we see that the city is missing in rows three and 6 (the 4th and 7th rows of the original data file).\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntricky1\n```\n:::\n\n\nWe can fix this with post processing:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntricky1[3, ] <- c(tricky1[3, 1:2], NA, tricky1[3, 3:4])\ntricky1[6, ] <- c(tricky1[4, 1], NA, tricky1[4, 3:5])\n```\n:::\n\n\n### tricky-2.csv\n\n-   `https://stat220-s25.github.io/data/tricky-2.csv`\n\nTo begin, we can attempt to read in the `tricky-2.csv` file:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntricky2 <- read_csv(\"https://stat220-s25.github.io/data/tricky-2.csv\")\nproblems(tricky2)\n```\n:::\n\n\nThis looks like a missing value problem again! Let's look at the rows with missing values:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntricky2 %>% slice(8:21)\n```\n:::\n\n\nWhy are there state abbreviations in the latitude column?!? (Because not all the rows put quotes around the \"City, State\" so it added an extra column!)\n\nOne solution is to parse the file in pieces. First, we can import the first 7 rows of the file:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntricky2_part1 <- read_csv(\n  \"https://stat220-s25.github.io/data/tricky-2.csv\",\n  n_max = 7\n)\n```\n:::\n\n\nNext, we parse the other rows, and manually specify the column names, since we are skipping the first row:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntricky2_part2 <- read_csv(\n  \"https://stat220-s25.github.io/data/tricky-2.csv\",\n  skip = 8, \n  col_names = c(\"iata\", \"airport\", \"city\", \"state\",  \"latitude\", \"longitude\")\n)\n```\n:::\n\n\nNow, we have to use some data wrangling skills to combine everything. First, we must create a `state` column in `tricky2_part1`\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntricky2_part1 <- tricky2_part1 %>%\n  separate(city, into = c(\"city\", \"state\"), sep = \",\")\n```\n:::\n\n\nThen, we need to bind the two sets of rows together using `bind_rows()`:\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntricky2 <- bind_rows(tricky2_part1, tricky2_part2)\n```\n:::\n\n\n# `read_excel` practice\n\nUse the appropriate `read_<type>()` function to import the following data sets:\n\n- `https://stat220-s25.github.io/data/sales.xlsx`\n\nStep 0: `read_excel` doesn't like URLs, so you'll first have to visit this link in a browser, download the .xlsx file to your computer, and save it in the same folder as your activity .Rmd file\n\nStep 1: read in the data so it looks like the following:\n\n```\n# A tibble: 9 × 2\n  id      n    \n  <chr>   <chr>\n1 Brand 1 n    \n2 1234    8    \n3 8721    2    \n4 1822    3    \n5 Brand 2 n    \n6 3333    1    \n7 2156    3    \n8 3987    6    \n9 3216    5   \n```\n\n\n::: {.cell}\n\n```{.r .cell-code}\nlibrary(readxl)\nsales<- read_excel(here(here(), \"data/sales.xlsx\"),\n           skip = 4,\n           col_names = c(\"id\", \"n\"))\n```\n:::\n\n\nStep 2 (Stretch goal): Manipulate the data so that it looks like the following:\n\n```\n# A tibble: 7 × 3\n  brand   id    n    \n  <chr>   <chr> <chr>\n1 Brand 1 1234  8    \n2 Brand 1 8721  2    \n3 Brand 1 1822  3    \n4 Brand 2 3333  1    \n5 Brand 2 2156  3    \n6 Brand 2 3987  6    \n7 Brand 2 3216  5  \n```\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsales |>\n  mutate(\n    brand = ifelse(n == \"n\", id, NA)\n  ) |>\n  fill(brand) |>\n  filter(n != \"n\") |>\n  select(brand, id, n)\n```\n:::\n\n\n\n# lubridate practice\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndesserts2 <- read_csv(\"https://stat220-s25.github.io/data/desserts.csv\")\n\ndesserts2 |>\n  mutate(\n    uk_airdate = dmy(uk_airdate),\n    how_long_ago = today() - uk_airdate,\n    how_long_ago = time_length(how_long_ago, unit = \"year\")\n  ) |>\n  select(\n    series, episode, uk_airdate, how_long_ago\n  )\n```\n:::\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}