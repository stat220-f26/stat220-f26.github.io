---
title: Intro to `ggplot2`
subtitle: "DRAFT — episodes (Rounds 1-3) + confessionals (Rounds 4-5)"
execute:
  eval: false
---

<!-- DRAFT in _playground/. If you like this direction: move to
activities/03-ggplot-intro.qmd, swap the ../data/ paths for the
https://stat220-f26.github.io/data/... pattern used elsewhere on the site
(see setup chunk), and delete this subtitle + this comment. -->

```{r setup, include=FALSE}
#| eval: true
q <- 0
```

::: callout-tip
## Note:

The YAML header includes the line `eval: false`. Make sure to delete this when you are ready to render your file.
:::

::: callout-tip
If you need a refresher for how to access the quarto file from this document, see [Activity01](01-college-tuition-pay.html)
:::

```{r}
#| out.width: 50%
#| fig.align: center
#| eval: TRUE
#| echo: FALSE

knitr::include_graphics("https://cdn.freebiesupply.com/logos/large/2x/survivor-logo-png-transparent.png")
```

The data we're using today contains information about all seasons of *Survivor* and comes from the [survivoR](https://github.com/doehm/survivoR) R package. In the show, a group of people (called castaways) are placed in an isolated location, where they must provide food, fire, and shelter for themselves. The castaways compete in challenges testing the contestants' physical abilities like running and swimming or their mental abilities like puzzles and endurance challenges for rewards and immunity from elimination. The castaways are progressively eliminated from the game as they are voted out by their fellow contestants until only two or three remain. At that point, the players who were eliminated (the "jury") vote for the winner. The winner is given the title of "Sole Survivor" and is awarded the grand prize of \$1,000,000

```{r}
#| eval: TRUE

survivor_episodes = readr::read_csv("../data/survivor_episodes.csv") # https://stat220-f26.github.io/data/survivor_episodes.csv
```

- `season`: the number of each season
- `version`: which country's version of the show (`US`, `AU`, `SA`, `UK`, `NZ`)
- `episode_number_overall`: the episode's position across the *entire* run of that version (episode 1 of season 1 is `1`, episode 1 of season 2 continues counting up, etc.)
- `episode`: the episode's position *within* its own season
- `episode_label`: `"Ep 1"`, `"Finale"`, `"Reunion"`, etc.
- `viewers`: number of people who watched the episode live
- `imdb_rating`: the episode's average IMDB rating
- `n_ratings`: how many IMDB users rated the episode

Two things to know about this data before you start: `viewers` is missing for
about a third of episodes (older or international episodes that were never
reported to Nielsen), and `imdb_rating`/`n_ratings` are missing for about 10%.
Missing data is normal — just something to notice, not something to fix today.

# Speed Groupwork

## Round 1: View the data and summary statistics

**`r (q <- q+1)`.** To get started, load the `{tidyverse}` and take a `glimpse` at the dataset. How many rows and columns are there? What does each row represent?

Loading the `{tidyverse}` loads 8 packages, one of which is `{ggplot2}`. You can certainly load each package individually, but it's often easier to just load the whole thing.

**`r (q <- q+1)`.** To access a column in the dataset, we can use `$`. For example, the chunk below selects the `viewers` column.

```{r}
#survivor_episodes$viewers
```

Run `summary(survivor_episodes$viewers)` and describe what you find. (What does the `NA's` line in the output mean?)

**`r (q <- q+1)`.** Another handy function is `unique()`. Run `unique()` on the `version` variable to see which countries have made their own version of *Survivor*.

::: callout-important
## Wait!

Stop here and wait for the next round of groups before moving on
:::

## Round 2: Scatterplots

Guess: over *Survivor*'s 25-year, 47-season run, has episode viewership gone up or down?

First, let's create a scatterplot of `viewers` (the number of people watching) vs. `episode_number_overall` (how far into the show's history that episode is).

A note on wording: when we say `viewers` vs. `episode_number_overall`, this should be interpreted as "variable on the y-axis" vs. "variable on the x-axis".

**`r (q <- q+1)`.** Fill in the data and aesthetic mapping in the below code chunk. What is returned? What's missing?

```{r}
# Fill in the blanks
ggplot(data = ___, mapping = aes(x = ___, y = ___))
```

**`r (q <- q+1)`.** Add the appropriate `geom`etric object to create the scatterplot. This is called adding a layer to a plot. Remember to always put the `+` at the end of a line, never at the start.

```{r}
# Copy your code from the previous chunk and add a geom
```

What do you notice? Was your guess right? Write a sentence or two describing your findings.

**`r (q <- q+1)`.** You must remember to put the aesthetic mappings in the `aes()` function! What happens if you forget?

```{r}
# Add a layer and see what happens
ggplot(data = ___, x = ___, y = ___)
```

**`r (q <- q+1)`.** The aesthetic mappings can be specified in the `geom` layer if you prefer, instead of the main `ggplot()` call. Give it a try:

```{r}
# Rebuild the scatterplot with your aesthetic mapping in the geom layer
ggplot(data = ___)
```

**Bonus, if you have time:** add a `geom_smooth()` layer to your scatterplot from question `r q-2`. Does the trend line back up your guess from the start of this round, or complicate it?

```{r}
# Add geom_smooth() to your scatterplot
```

::: callout-important
## Wait!

Stop here and wait for the next round of groups before moving on
:::

## Round 3: Additional Aesthetics

`x` and `y` are not the only aesthetic mappings possible. In this section you'll explore the `color`, `size`, `shape`, and `alpha` (i.e. transparency) aesthetics — this time on a different question: **does a better-reviewed episode draw more viewers, or are ratings and popularity unrelated?** Make a guess before you start plotting.

**`r (q <- q+1)`.** Create a scatterplot of `viewers` vs. `imdb_rating`. Add the `color` aesthetic to map `version` to the point color.

```{r}
ggplot(data = ___) +
  geom_point(aes(x = ___, y = ___, color = ___))
```

Does the plot support your guess?

**`r (q <- q+1)`.** Create a scatterplot of `viewers` vs. `imdb_rating`. Use `shape` to represent `version`. Is this plot easier or harder to interpret than the previous plot?

**`r (q <- q+1)`.** Create a scatterplot of `viewers` vs. `imdb_rating`. Use both `shape` and `color` to represent `version`. Is this plot easier or harder to interpret than the previous two plots?

**`r (q <- q+1)`.** Create a scatterplot of `viewers` vs. `imdb_rating`. Use `color` to represent the `season`. What did you learn from the plot?

**`r (q <- q+1)`.** Create a scatterplot of `viewers` vs. `imdb_rating`. Use `size` to represent the `season`.

**`r (q <- q+1)`.** Look back at your scatterplots from the last few questions. Explain the differences when you map aesthetics to discrete and continuous variables.

**`r (q <- q+1)`.** Create a scatterplot of `viewers` vs. `imdb_rating`. Use `alpha` to represent the `season`.

**Bonus, if you have time:** a lot of episodes share the same rounded IMDB rating, so points are stacking on top of each other. Try `geom_count()` instead of `geom_point()` (same syntax!) on the `viewers` vs. `imdb_rating` scatterplot. What does the size of each point represent now?

```{r}
# Same aes() as before, but with geom_count()
```

::: callout-important
## Wait!

Stop here and wait for the next round of groups before moving on
:::

## Round 4: Visualizing Distributions

For the rest of today we'll switch to a different table from the same package: `confessionals`, which has one row per castaway per episode, recording how many on-camera "confessional" interviews they gave.

```{r}
#| eval: TRUE

survivor_confessionals = readr::read_csv("../data/survivor_confessionals.csv") # https://stat220-f26.github.io/data/survivor_confessionals.csv
```

- `season`, `version`: same as before
- `era`: which stretch of the show's history the season falls in (`"Seasons 1-15"`, `"Seasons 16-30"`, `"Seasons 31+"`)
- `episode`: episode number within the season
- `castaway`: the castaway's name
- `confessional_count`: how many confessionals that castaway gave in that episode

**`r (q <- q+1)`.** Build a histogram of `confessional_count` using `geom_histogram()`. Don't hesitate to look at the ggplot2 cheat sheet for help!

```{r}
# Fill in the blanks
ggplot(___) +
  geom_histogram(aes(x = ___))
```

What have you learned about the distribution of confessional counts?

**`r (q <- q+1)`.** By default, `ggplot2` uses 30 bins. To change the number of bins, to say 15, add the argument `bins = 15` to `geom_histogram()`. Note: this is not an aesthetic mapping.

```{r}
# Fill in the blanks
ggplot(___) +
  geom_histogram(aes(x = ___), bins = ___)
```

**`r (q <- q+1)`.** Instead of a histogram, let's create a kernel density plot. To do this, substitute geom_density() into your code for question `r q-2`.

**`r (q <- q+1)`.** Prediction: as *Survivor* has racked up more seasons and (usually) more castaways per season, do you think the typical number of confessionals per person has gone up, gone down, or stayed about the same? Make your guess, then make side-by-side boxplots of `confessional_count` for each `era`.

```{r}
# Fill in the blanks
ggplot(___) +
  geom_boxplot(aes(x = ___, y = ___))
```

**`r (q <- q+1)`.** A violin plot is a kernel density on its side, made symmetric. Change your code from question `r q-1` to use `geom_violin()`. Which plot do you prefer, boxplots or violin plots? Why?

```{r}
# Put your violin plot code here
```

**Bonus, if you have time:** try `geom_jitter()` on top of (or instead of) your boxplot/violin from the last two questions. A lot of castaways get 0 or 1 confessionals in a given episode — what does jitter show you about that pile-up that the boxplot and violin plot hide?

```{r}
# Add or swap in geom_jitter()
```

::: callout-important
## Wait!

Stop here and wait for the next round of groups before moving on
:::

## Round 5: Bar and column charts + Labeling

How many confessional records do we have for each version of the show? Let's find out!

**`r (q <- q+1)`.** Make a bar chart of the number of rows for each `version` using `geom_bar()`

```{r}
# Fill in the blanks
ggplot(___) +
  geom_bar(aes(x = ___))
```

**`r (q <- q+1)`.** When you have lots of categories, it's sometimes hard to read the labels on the x-axis. One trick is to flip the axes. Rebuild your bar chart using the `y` aesthetic instead of `x`.

```{r}
# Fill in the blanks
ggplot(___) +
  geom_bar(aes(y = ___))
```

**`r (q <- q+1)` .** Sometimes the variable we want to show up in the bar chart shows up explicitly in our data, so we don't need `geom_bar()` to count it for us. The chunk below creates `finale_confessionals`, which contains just the castaways from the Season 40 ("Winners at War") finale and how many confessionals each one gave *in that episode*.

```{r}
#| eval: TRUE

finale_confessionals = survivor_confessionals |>
  dplyr::filter(season == 40, episode == max(episode[season == 40]))
```

Put `confessional_count` on the x-axis, and `castaway` on the y-axis. Use `geom_col` (column) as the geom, using the `finale_confessionals` data. How is this different than a bar plot?

```{r}
# Fill in the blanks
ggplot(finale_confessionals) +
  geom_col(aes(x = ___, y = ___))
```

Who got the most confessional time in that episode? Does it match who you'd guess actually won the season? (No spoilers if you don't want them — but the answer's right there in your plot's y-axis order, or lack thereof... notice the bars aren't sorted by value. We'll fix that with `forcats` in a few weeks.)

**`r (q <- q+1)`.** In ggplot2 you can add/change the title, subtitle, caption, and x- and y-axis labels by adding a labs() layer. Below is an example illustrating it's use. Choose one graph from today and add all labels.

```{r}
ggplot(data = mpg) +
  geom_point(mapping = aes(x = displ, y = hwy)) +
  labs(
    title = "Put your informative title here",
    subtitle = "and your subtitle here",
    x = "New x label",
    y = "New y label",
    caption = "Put a caption here"
  )
```


::: callout-important
## When you're done

Make a post on the "Week 1 - Friday" discussion thread on Ed.

- Option 1: If you have any lingering questions about ggplot, feel free to post them
- Option 2: You can also post your final graph for the last question!
:::
